services:
  # Наш Nuxt фронтенд
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nuxt-app
    restart: always
    env_file:
      - .env
    ports:
      - "3000:3000"